# TODO

- Check for entity groups under the Database level. It sits between the DB and table layers.
- Fix logging for when Auth has initiated
- check if regex patterns in blacklist function are actually working
- empty table lists are being processed for blacklist removal, causing an error as -inputList is $null


